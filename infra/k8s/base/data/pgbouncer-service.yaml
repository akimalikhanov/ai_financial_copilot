apiVersion: v1
kind: Service
metadata:
  name: pgbouncer              # <- the name the app dials (APP_DB_HOST=pgbouncer)
  namespace: copilot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pgbouncer
  ports:
    - name: pgbouncer
      port: 6432
      targetPort: 6432

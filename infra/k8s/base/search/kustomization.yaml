apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - qdrant-service.yaml
  - qdrant-statefulset.yaml
  # later phases add: opensearch, opensearch-dashboards, elasticsearch, es-bootstrap Job

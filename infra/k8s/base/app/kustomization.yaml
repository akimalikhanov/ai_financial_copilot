apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - api-deployment.yaml
  - frontend-deployment.yaml
  - worker-chat-deployment.yaml
  - worker-ingestion-deployment.yaml
  - tei-deployment.yaml
  - tei-embedder-deployment.yaml
  - model-preload-job.yaml

configMapGenerator:
  - name: app-config
    envs: [app-config.env]      # committed — non-secret tuning knobs
  - name: app-endpoints
    envs: [app-endpoints.env]   # committed — in-cluster hosts/ports

generatorOptions:
  disableNameSuffixHash: false

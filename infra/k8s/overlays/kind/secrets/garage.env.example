# Keys only, empty values — COMMITTED. Real values live in the gitignored garage.env,
# generated by `make k8s-secrets`. The two garage_s3_* keys are pre-generated and imported by
# the garage-bootstrap Job (stage-17 plan Phase 9, concept 4) rather than created at runtime.
garage_rpc_secret=
garage_admin_token=
garage_metrics_token=
garage_s3_access_key_id=
garage_s3_secret_access_key=

# Keys only, empty values — COMMITTED. Real values live in the gitignored app.env,
# generated by `make k8s-secrets`. AWS_* is written here by Phase 9 (copied from garage.env's
# pre-generated S3 key pair); more keys (JWT_SECRET, LLM provider keys, ...) land once the app
# tier phase adds them and wires app.env into an app-secret secretGenerator.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

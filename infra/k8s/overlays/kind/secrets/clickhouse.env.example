# Keys only, empty values — COMMITTED. Real values live in the gitignored clickhouse.env,
# generated by `make k8s-secrets` (stage-17 plan Phase 16). The ClickHouse StatefulSet and
# both Langfuse pods read this same Secret, so the password can never diverge between them.
CLICKHOUSE_USER=
CLICKHOUSE_PASSWORD=

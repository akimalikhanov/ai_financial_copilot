# Keys only, empty values — COMMITTED. Real values live in the gitignored postgres.env,
# generated by `make k8s-secrets` (Makefile target not yet added — see stage-17 plan Phase 2).
POSTGRES_USER=
POSTGRES_PASSWORD=
APP_DB_PASSWORD=
LANGFUSE_DB_PASSWORD=
